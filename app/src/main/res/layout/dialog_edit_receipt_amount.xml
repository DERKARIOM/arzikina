<?xml version="1.0" encoding="utf-8"?>
<!-- Contenu du dialogue "Modifier le montant" (voir ReceiptDetailFragment.showEditAmountDialog) —
     même structure que dialog_rename_receipt.xml (champ clair standard, PAS le style "postcard"
     sombre). android:inputType="numberDecimal" + Money.parseToMinorUnits/formatMajorUnits pour la
     saisie/validation, même convention que tous les champs Montant de l'application (voir
     TransactionFormFragment, BudgetFormFragment...). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="@dimen/spacing_l"
    android:paddingTop="@dimen/spacing_s">

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/editAmountLayout"
        style="@style/Widget.Material3.TextInputLayout.OutlinedBox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/receipt_detail_edit_amount_hint">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/editAmountInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="numberDecimal"
            android:maxLines="1" />

    </com.google.android.material.textfield.TextInputLayout>

</LinearLayout>
