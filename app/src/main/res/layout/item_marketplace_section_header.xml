<?xml version="1.0" encoding="utf-8"?>
<!--
    En-tête de section "⭐ Mes favoris" / "Tous les modèles" (voir MarketplaceListRow.SectionHeader,
    cahier des charges section 7) — même structure conteneur que item_recurring_section_title.xml
    (TextView enfant avec id, jamais un root unique avec id, pour un binding sans ambiguïté).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="@dimen/spacing_m">

    <TextView
        android:id="@+id/sectionTitle"
        style="@style/TextAppearance.Material3.TitleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_l"
        android:textColor="?attr/colorOnSurface"
        android:textStyle="bold"
        tools:text="⭐ Mes favoris" />

</LinearLayout>
